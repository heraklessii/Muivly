{
  "$schema": "../gen/schemas/windows-schema.json",
  "identifier": "default",
  "description": "What the settings window is allowed to call. Commands defined by Muivly itself are not listed here; only plugin commands go through the ACL, and the file picker is the one plugin this app uses — opening files to add to the library, and saving a .muivly package back out.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save"]
}
